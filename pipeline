pipeline{
    agent any
    stages{
        stage('CHECKOUT'){
            script{
                 checkout scm

            }
        }
        stage('BRANCH_NAME'){
            script{
                fetch 
                 echo "Current branch: ${env.GIT_BRANCH}"
            }
        }

    }
}
